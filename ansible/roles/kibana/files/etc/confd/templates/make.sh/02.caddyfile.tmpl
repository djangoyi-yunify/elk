cat > /opt/app/conf/caddy/Caddyfile << KIBANA_CADDYFILE_EOF
{{ $myIp }}:80 {
  root /data
  gzip
  browse /kibana/logs
  browse /haproxy/logs
  browse /keepalived/logs
  tls off
}
KIBANA_CADDYFILE_EOF

